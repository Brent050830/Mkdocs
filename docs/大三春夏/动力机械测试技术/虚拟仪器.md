## 标准化
### 虚拟化
基于通用硬件平台，充分利用软件定义

### 常用虚拟仪器系统开发语言
- 标准 C
- C++,C#,VB.net
- LabVIEW 图形化编程语言（G 语言）
 
### 系统开发软件环境
G 语言，NI Labview

- 基于配置的助手工具简化开发
- 多种模块和工具包加速应用开发

### LabView
本质是一种编程语言，基于数据流的

#### 一个简单而典型的应用

### 组成
#### 前面板
#### 程序框图
### 程序设计
#### 数据流
- 原则 1：只有当一个节点的所有输出端（从左到右执行的）

采集——分析——表达

移位寄存器  
延时环节  
那些语句都有框图（框图内的就相当于在这个语句中执行的子语句）  
状态转移

help 文档

通道配时、时间配置、采集、任务清除、储存等等（在循环中记录数据）

## 初识 labview
页面：![](png/Pasted%20image%2020260522231855.png)  
这里的程序叫做 VI，所以新建的时候点击文件，新建 VI

- 前面的方框的部分为**前面板**
- 后面的为程序框图（后面板）  
	可以通过 ctrl+e 切换两者
- 前面板：放置用户界面
- 后面板：编写程序的

### 简单加法小程序
#### 前面板
- 右键前面板：出现控件的框  
	![](png/Pasted%20image%2020260522232212.png)
	- 分为两类：用户输出的和显示给用户的

---

- 数值——>数值输入控件——>放置到前面板，可以给他改名字  
	你就会发现程序框图面板多出了一个数值的控件  
	可以通过双击某个面板上的图标定位到另一个面板上的对应图标  
	按住 ctrl 拖动也可以复制控件
- 数值——>数值输出控件
- 程序面板：右键、数值、加法  
	之后进行连线就行（程序框图中）  
	![](png/Pasted%20image%2020260522233103.png)  
	类似于 simulink
- 单击左上角的小箭头![](png/Pasted%20image%2020260522233247.png)就能运行
- 点击文件 - 保存即可保存
- 之后可以打开，但是默认的只有前面板，仍然使用 `ctrl+e ` 键调出来

## 编程特点
遇到的编译错误是非常少的  
因为是通过连线的，所以语法编译错误是非常少的

### 天然并行性运行
### 基于数据流运行
查看数据运行的过程：![](png/Pasted%20image%2020260523114525.png)  
使用这个小灯泡，点击运行就能看见运行过程（按照线运行的，类似于电流的方式）  
而且这两个数据流是同时流动的（**并行**）  
![](png/Pasted%20image%2020260523114917.png)  
类似这样的两块程序，这两块程序也是同时执行的（没有用线连起来的两块是同时运行的，但是用线连起来的两个是基于数据流运行的）

> [!important]+ 文本语言  
> 文本语言是从上往下执行的，而我们的 labview 是天然并行的

---
那怎么让按照顺序执行呢：

- 函数——结构——平铺式顺序结构  
	按住拖动（画一个长方形）  
	右键，在后面添加帧  
	![](png/Pasted%20image%2020260523115417.png)  
	这样就可以了（先执行前面的，后执行后面的）

> [!tip]+ 小技巧  
> 选中某个控件，然后右键可以；对其进行一些操作

要想延时执行的话，可以在中间加一个帧，放一个延时环节：定时——延时；常量——数值常量  
选中所有的程序，然后点击中间的小扫把可以整理：  
![](png/Pasted%20image%2020260523120127.png)

## 程序结构 1（循环结构）
### 顺序结构
这是 labview 独特的语句  
同样的，在结构中找到顺序结构

#### 案例
控制布尔圆形指示灯亮灭（在前面板中）  
在程序框图中找到真值或者假值（单击可以切换真假值）

> [!tip]+ 对于一个控件如何在多个位置进行复制  
> 右键——创建——属性节点——值  
> 右键这个控件  
> 全部转换为写入  
> 就相当于又能给同一个控件赋予不同的值了

除了平铺式顺序结构还有层叠式顺序（右键这个框，替换为层叠式），右键，替换，可以回来  
![](png/Pasted%20image%2020260523133030.png)  
控制一个灯亮灭亮灭

### for 循环
在程序框图中找到结构中的 for 循环（也是拖动）

- 左上角 N，左边右键创建常量  
	![](png/Pasted%20image%2020260523133651.png)  
	这样，N 为循环总次数  
	I 为当前的循环次数  
	可以在前面板中加入数值显示控件，i 输出给这个控件，可以看见循环的次数了  
	![](png/Pasted%20image%2020260523133855.png)  
	也可以直接右键 i 的输出端，创建显示控件
- 前面板  
	右键，图表，波形图表（显示数据使用的）
- 在程序框图中找到随机数，给到波形图表  
	![](png/Pasted%20image%2020260523134655.png)

### while 循环
同样的，程序框图，结构，while 循环  
![](png/Pasted%20image%2020260523134826.png)  
左下角——运行次数  
右下角——控制运行（幅值假常量，保持循环，赋值真常量，退出循环）  
![](png/Pasted%20image%2020260523135621.png)

## 程序结构 2
### 条件结构
if else

- 前面板中找到布尔——开关按钮
- 在程序框图中找到结构——条件结构
	- 将这个布尔开关的输出给到条件结构的输入
	- 条件结构中间上面有真和假的分支
- 前面板中找到字符串的显示控件，给他一个字符串的常量
- 切换到假分支中，对字符串的显示控件创建一个值，并仅仅输入
- 再加一个 while 循环，就会得到这样的程序：  
	![](png/Pasted%20image%2020260523180550.png)

---
条件结构的输入还可以是**整形和字符串**类型（此时就相当于 switch case 了）

- 前面板——数值输入控件
	- 这个默认是输入小数的，可以右键，表示法，选择整形（int）
	- 然后就变蓝了
- 然后将这个输入控件与条件语句相连，上面的就变成 0,1 了
- 现在自动创建了两个分支（相当于一个 if else）
- ![](png/Pasted%20image%2020260523181311.png)
	- 现在这个就代表输入是 1 的时候输出为我是 1
	- 输入不是 1 的时候到默认那里，输出为我不是 1
- 想多几个条件的话（在前后添加分支，现在就相当于 switch case 了）  
	默认分支是必须有的  
	设置为其余的话，可以先将之前的默认的右键取消掉，设置为新的

字符串类型也是相似的，就是 switch case  
![](png/Pasted%20image%2020260523182138.png)

### 事件结构
新的：布尔中的确认按钮

点击中间的部分，右键，**添加事件分支**

- 打开按钮——值改变
- 关闭按钮——值改变  
	同样也是在不同的分支下进行小灯数值的改变  
	![](png/Pasted%20image%2020260523213517.png)![](png/Pasted%20image%2020260523213625.png)  
	这样子的，也是类似于条件语句  
	这个的左上角就是等待的时间（事件产生的时间），默认是无限的时间  
	超时的用法（超时之后会直接运行结束）  
	![](png/Pasted%20image%2020260523214416.png)  
	这样的  
	一般事件结构的外面会放 while 循环，但是里面不会放 while 循环（一般）

### 禁用结构
就是注释  
![](png/Pasted%20image%2020260523215100.png)  
将某些地方屏蔽掉  
点一下还会启用，可以加入新的东西覆盖掉

## 数值、布尔
![](png/Pasted%20image%2020260523235031.png)  
这些数是通用的

数值输入中——右键——表示法即可表示为整型（在程序框图中会变颜色）  
常数分为整型和小数类型，也是可以相互转换的（常数也可以转变为输入控件），反正就是可以来回转换

要符合大小

---
由于浮点算术运算可能存在误差，所以不能作为条件结构的输入（因为是使用有限位数的二进制表示的，所以有的浮点数无法精确表示）  
输入浮点数的话会报错

### 枚举类型
在前面板中找到枚举选项  
右键，插入项就可以插入项，通过上下移可以调整位置  
类似于一个字典，键值对  
可以直接接入条件语句中  
![](png/Pasted%20image%2020260524161254.png)

---
下拉列表更加接近字典，能够编辑对应的数值，而不是简单的 1,2,3,  
![](png/Pasted%20image%2020260524161501.png)  
所以下拉列表比枚举更加强大（也是可以连接到条件结构的）

---
也有枚举常亮和下拉列表常量

### 布尔
布尔作为输入控件的时候在运行的时候才能改变

## 字符串类型
### 字符串的本质
一堆二进制的代码  
同样，字符串控件和字符串常量（可以相互转换的）  
它是可以 16 进制显示的（acsll 码）  
数据中有强制类型转换，可以将数值类型转变为字符串类型

---
字符串长度  
单个汉字的长度为 2 字节

- 字符串截取
	- 字符串
	- 偏移量
	- 截取长度
- 字符串的合并
	- 可以直接拉长，增加连接的字符串
- 行结束常亮（字符串），可以实现字符串的换行
- 数值字符串转换为数值类型  
	到字符串  
	数值，字符串转换  
	分数字符串至数值转换  
	![](png/Pasted%20image%2020260524164158.png)
- 反转过来  
	![](png/Pasted%20image%2020260524164310.png)

字符串相关的函数还是很多的，自己看着学习一下

## 数组
### 创建
- 右键——数据容器——数组
- 之后在数值中选择，数值输入控件，将该控件拖入到数组中
- 这就是数组的输入控件  
	![](png/Pasted%20image%2020260524165819.png)  
	如图，都是分为两步，先放数组的结构体，之后放置类型控件
- 类似的，在程序框图中可以创建数组常亮和字符串数组常亮  
	而且也是可以相互转换的
- 数组的定义：  
	![](png/Pasted%20image%2020260524170428.png)  
	左边的数代表有几维（如图有两维）（数字代表数组的索引）  
	然后上下左右拉就行

### 控件
- 数组的大小控件（最后的输出为一个数组）
- 利用位置索引的（数组索引函数）

### for 循环
将数组连到 for 循环，此时就制定了其运行的次数（就是数组的元素个数）  
而且还可以将数组中的元素索引出来

---
for 循环还可以自动生成数组  
通过这个随机生成数组  
![](png/Pasted%20image%2020260524174924.png)

---
右键——数据操作——当前值设置为默认值

## 蔟
将多种类型的数据集成在一个数据结构中

### 创建
同样的，找到蔟，然后往里面放元素  
但是蔟里面是可以放**不同类型的元素的**

可以转换为显示或者输入控件（蔟是什么控件，蔟里元素的控件类型就是什么）  
而且蔟的这个类型由放入的第一个元素的类型决定的，之后放置的就无所谓了，都同化为第一个的种类了

蔟也是有常量的，放入的数据也是常量

---

### 蔟函数
#### 接触捆绑
蔟——接触捆绑函数  
![](png/Pasted%20image%2020260524210056.png)

类型和定义的类型是一样的（接触之后显示的顺序和放置时候的顺序一样）  
可以右键，重排顺序：（按顺序单击即可）  
![](png/Pasted%20image%2020260524210309.png)

但是这个只能提取出所有的元素

---
按照需要提取的话：  
按名称接触捆绑  
![](png/Pasted%20image%2020260524210608.png)  
想获取什么，点击一下选择就行，多个的话，拉取即可（程序框图的蔟常亮是没有名字的）  
右键——显示项，然后起名字即可

#### 捆绑函数
![](png/Pasted%20image%2020260524211132.png)  
使用蔟捆绑函数

还可以使用这个**修改某个蔟**  
![](png/Pasted%20image%2020260524211437.png)  
连到中上的线，改了想要改的地方

---
同样有着按名称捆绑  
![](png/Pasted%20image%2020260524211740.png)

形成了新的蔟（实际上原来蔟的元素没有变化）

### 蔟的错误
复杂的错误输出是一个蔟  
![](png/Pasted%20image%2020260524212115.png)  
也就是将错误记录下来  
输出这样的一个蔟：  
![](png/Pasted%20image%2020260524212207.png)

然后按照名称将这个蔟的关键部分提取：  
![](png/Pasted%20image%2020260524212427.png)  
而且这个条件结构可以直接连接错误簇：  
![](png/Pasted%20image%2020260524212615.png)

右键、数据操作、当前值设为默认值

## 变量的调用
### 直接读取/赋值
使用相应的控件即可，但是不能同时幅值和读取  
所以用到属性节点

### 属性节点
比如控制循环的亮灭：  
![](png/Pasted%20image%2020260525224039.png)

![](png/Pasted%20image%2020260525224250.png)  
还可以类似这种的方式，B 只 用来定义，之后的幅值和输出都使用其属性节点

### 局部变量
右键——结构——局部变量  
![](png/Pasted%20image%2020260525224507.png)  
然后将局部变量的值改为 C(就相当于一个中间、局部变量)

还可以这样，类似于属性节点值：  
![](png/Pasted%20image%2020260525224704.png)

创建局部变量速度快，简单（就相当于文本编程中多次使用变量名）

### 全局变量
多个 VI 程序之间调用

## 数据采集
在测量 I/O 中找到 DAQ 测量![](png/Pasted%20image%2020260526231834.png)  
固定一下

- 创建**虚拟通道**：（右上角的问号可以提供帮助）  
	![](png/Pasted%20image%2020260526231930.png)
	- 给输入接线端创建输入控件（配置输入信号是什么信号）  
		RES：单端  
		差分  
		伪差分  
		由使用的传感器输入
	- 最小值最大值（自己定）
	- 任务输入：就使用面板中的开始任务选项
	- 物理输入：输入我们的数据采集卡（我们创建了一个虚拟的数据采集卡）
		- 在 nimax 中创建——数据与接口![](png/Pasted%20image%2020260527104835.png)
		- 然后创建物理输入，在前面板中选择我们的数据通道即可
- 然后是采样时钟的设置：  
	![](png/Pasted%20image%2020260527105404.png)
	- 将前者的两个输出接到采样时钟的两个输入
	- 每通道采样：每次 DAQ 从计算机缓存中读取的数据量（一般大一点）
	- 采样模式：创建输入，一般选择连续采集
	- 速率：采样的频率
	- 源：参考的时间，选择第一个比较好
- 然后就是 DQA 开始任务  
	![](png/Pasted%20image%2020260527105948.png)
	- 同样的，它的任务、错误输入接入时钟的输出
- 然后是读取模块：  
	![](png/Pasted%20image%2020260527110054.png)
	- 也是接上一个的任务和错误
	- 下拉框选择多通道，多采样![](png/Pasted%20image%2020260527110224.png)
	- 然后数据——创建显示通道
	- 每通道采样数（这里才是控制的，也是设置为 1000）（也可以设置为 -1.尽可能多采集）
- 还需要建立 while 循环连续采集  
	将读取的部分用 while 循环框住：![](png/Pasted%20image%2020260527110603.png)
- 前面板：  
	![](png/Pasted%20image%2020260527110648.png)  
	以速率 1000 读取，然后在通道 ai 0 输入，之后存储到计算机的缓存区中  
	之后每次拿取 1000 个数据（没有 while 循环的话拿一次就结束了，现在有 while 循环，一次拿一千）（拿取的数据太少的话，缓存区容易满）  
	还可以加一个波形布标实时显示数据

- 文件 i/o：写入测量文件  
	![](png/Pasted%20image%2020260527112114.png)  
	![](png/Pasted%20image%2020260527112422.png)  
	这样子的

![](png/Pasted%20image%2020260527113910.png)  
这就是最后的程序

### 移位寄存器
1. 右键单击循环的左侧或者右侧的边框，选择添加移位寄存器（循环中都可以使用）
2. 一维寄存器用于将上一次训话产生的值传递至下一次循环，移位寄存器以成对接线端的形式出现，分别位于循环两侧的边框上，位置相对
3. 右侧接线端含有一个向上的箭头，用于存储每次循环结束时的数据
4. 移位寄存器可以传递任何类型的值
5. 初始化以为寄存器：将一个输入控件或者常数联接至移位寄存器接线端
6. 一个循环中可以有多对移位寄存器  
	![](png/Pasted%20image%2020260527172731.png)  
	就是左右的这个上下箭头

层叠移位寄存器：可以使用前面 n 次循环时的值  
![](png/Pasted%20image%2020260527173003.png)  
现在层叠了两个，第二个输出的就是使用的是上上次的值（两个都得初始化）