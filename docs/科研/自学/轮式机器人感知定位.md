## 概念
### 典型自主控制系统

```text
           激光雷达 / 相机 / IMU / 编码器
                       │
                       ↓
                  ROS 2 驱动
                       │
        ┌──────────────┴──────────────┐
        ↓                             ↓
   里程计 / IMU                  LiDAR / Camera
        │                             │
        └───────────┬─────────────────┘
                    ↓
              Localization / SLAM
             定位 + 建图 + TF
                    │
                    ↓
                  Map
                    │
              ┌─────┴─────┐
              ↓           ↓
          Global Map   Local Map
              │           │
              ↓           ↓
       Global Planner   障碍物
        A*/Dijkstra       │
              │           │
              └─────┬─────┘
                    ↓
             Local Planner
            / Controller
       DWB / MPPI / MPC...
                    │
                    ↓
               cmd_vel
                    │
                    ↓
              底盘控制器
                    │
                    ↓
                电机
```

> 之前做的 CARLA 路径规划 + MPC 轨迹跟踪非常接近，只不过 ROS 把感知、定位、地图、规划、控制等模块用**统一通信框架**连接起来。

### ROS
机器人控制系统，不是传统意义上的操作系统，而是：机器人软件的通信框架 + 工具集 + 软件生态

```text
激光雷达程序
      ↓
SLAM程序
      ↓
路径规划程序
      ↓
MPC控制程序
      ↓
电机驱动程序
```

这些程序可以分别作为 ROS 的 Node（节点）

节点之间通过：

```text
Topic
Service
Action
Parameter
TF
```

等等进行通信

现在学习的一般是 ROS 2

---

### SLAM
Simultaneous Localization And Mapping：同时定位与建图

机器人进入未知环境中：不知道地图 + 不知道自己在哪里

通过：

```text
LiDAR
Camera
IMU
Wheel Odometry
```

不断估计机器人的 x,y, $\theta$，同时建立 map

### Planning
SLAM 告诉机器人：

> “你在这里，地图长这样。”

Planning 则回答：

> “那我要怎样从 A 走到 B？“”

就是轨迹规划  
经典算法：

```text
Dijkstra
A*
Theta*
Hybrid A*
RRT
RRT*
```

### control
由给出的跟踪轨迹的坐标，计算出所需的控制量，让机器人真正跟踪轨迹

常见的为 PID 控制  
再往后为：

```text
Pure Pursuit
Stanley
LQR
MPC
MPPI
```

---
以下进入小车的项目：

## 项目的整体结构

```text
catkin_ws/src/
│
├── sc_mini
│      ↓
│   激光雷达底层驱动
│
├── sensor_lidar
│      ↓
│   雷达滤波、LaserScan→PointCloud、聚类
│
├── sensor_camera
│      ↓
│   CSI摄像头
│
├── perception_vision
│      ↓
│   YOLOv8 + TensorRT
│
├── fusion_pkg
│      ↓
│   雷达 + YOLO 融合【目前未完成】
│
├── base_controller
│      ↓
│   cmd_vel → 油门/转向 → PCA9685
│
├── jetson_cam
│      ↓
│   另一套摄像头驱动/旧代码
│
└── jetson-utils
       ↓
    NVIDIA Jetson第三方库
```

目前的一条数据流为：

```text
                ┌──────── CSI Camera ──────────┐
                │                              │
                ↓                              ↓
       /camera/image_raw                  YOLOv8 TensorRT
                                               │
                                  ┌────────────┴────────────┐
                                  ↓                         ↓
                         /yolo/detections           /yolo/image_raw
                                  │
                                  │
                                  ↓
                            fusion_node
                                  ↑
                                  │
SC Mini ─→ /scan ─────────────────┘
   │
   ↓
laser_filters
   │
/scan_filtered
   │
   ↓
scan_to_cloud_node
   │
/cloud_filtered
   │
   ↓
PCL Euclidean Clustering
   │
/cloud_clustered
```

另外的一条控制链为：

```text
某个未来的规划/控制器
        │
     /cmd_vel
        ↓
base_controller
        │
        ├── /motor/throttle_target
        │
        └── /motor/steering_target
                  ↓
             i2c_bridge
                  ↓
              PCA9685
                  ↓
          舵机 / 电机控制
```

> 但是现在的上游还没有真正的路径规划器

## 激光雷达的接入
这部分的代码比较清楚  
这部分代码比较清楚。

> 1. sc_mini

这是 SC Mini 二维激光雷达的底层驱动。

启动文件：

```python
sc_mini/launch/start.launch
```

里面设置了：

```python
frame_id = laser_link  
port = /dev/ttyUSB0  
baud_rate = 115200
```

然后驱动发布：

```python
/scan
```

消息类型：

```python
sensor_msgs/LaserScan
```

所以这一部分实际上已经解决：

```text
物理雷达  
  ↓  
串口  
  ↓  
SC Mini Driver  
  ↓  
ROS /scan
```

这个属于计划第一周“传感器驱动部署”的内容。计划要求雷达能在 ROS 中稳定输出数据并可视化。

### 雷达滤波部分

```python
sensor_lidar
```

为雷达滤波的部分
输入为 `/scan`，经过 `laser_filters` 变成 `/scan_filtered`，配置文件为：`sensor_lidar/config/scan_filter_chain.yaml`
其中有着四种滤波：

1. 距离滤波：`0.12 m ～ 8.0 m`
2. 孤立点滤波：`ScanSpeckleFilter` 用于去除飞点
3. 中值滤波